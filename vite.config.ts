import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/LaunchPad-React-TS/'  // Set the base path for GitHub Pages deployment
})
